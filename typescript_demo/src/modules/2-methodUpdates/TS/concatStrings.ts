//what happens if we update the parameters of the function?

export const concatStrings = (str1: string, str2: string): string => {

    alert(`concatStrings: ${str1} ${str2}`);
    return str1 + str2;
}